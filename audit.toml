[advisories]
ignore = [
  # paste crate is unmaintained (RUSTSEC-2024-0436). It reaches the tree only
  # through the leptos family (search Cargo.lock for `paste`). rmcp uses pastey.
  # Remove this entry when leptos drops it.
  "RUSTSEC-2024-0436",
]
