[advisories]
ignore = [
  # paste crate is unmaintained (RUSTSEC-2024-0436). It now reaches the tree
  # through leptos (tachys, reactive_graph, either_of), not rmcp: rmcp moved to
  # pastey and the 1.x upgrade confirmed it. Track leptos for removal.
  "RUSTSEC-2024-0436",
]
